{
  "name": "add-param-from-cookie-cf",
  "version": "1.1.0",
  "description": "Add param from cookie to query to create separate cache",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cookie": "^0.5.0"
  }
}
